#/bin/bash

logfile="get_nmon.log"

touch $logfile

date >> $logfile
echo " ----------deploy01------------- " >> $logfile
sudo su - fbadmin -c "ssh -l root deploy01.fleetboard.dmz 'cd /home/fbadmin/; ls -al'" >> $logfile

echo " ----------build01------------- " >> $logfile
sudo su - fbadmin -c "ssh -l root build01.es.fleetboard.dmz 'cd /home/fbadmin/; ls -al'" >> $logfile

echo " ----------databases------------- " >> $logfile
sudo su - fbadmin -c "ssh -l root zu5rd005.fleetboard.dmz 'cd /home/fbadmin/; ls -al'" >> $logfile

echo " ----------svn------------- " >> $logfile
sudo su - fbadmin -c "ssh -l svnadm svn.fleetboard.dmz 'cd /home/svnadm/svnadmin; ls -al'" >> $logfile
echo " ----------svn user rights------------- " >> $logfile
#sudo su - fbadmin -c "ssh -l svnadm svn.fleetboard.dmz 'cat /home/svnadm/svnadmin/trunk/public-svn-authzfile'" >> $logfile

echo " ---------------------------- " >> $logfile
echo "Finished @" >> $logfile
date >> $logfile

cat $logfile | mail -s "log" fl33tb0ard@gmail.com

rm $logfile

exit 0
